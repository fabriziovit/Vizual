<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".fragment.ZoomFragment"
    android:background="#FFFEEF">

    <com.ortiz.touchview.TouchImageView
        android:id="@+id/zoomImageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabZoom25"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginBottom="110dp"
        android:layout_marginEnd="12dp"
        app:backgroundTint="@color/colorPrimary"
        android:outlineAmbientShadowColor="@color/colorPrimary"
        app:fabSize="mini"
        app:tint="@color/white"
        app:srcCompat="@drawable/ic_zoomx2"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabZoom15"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginBottom="65dp"
        android:layout_marginEnd="12dp"
        app:backgroundTint="@color/colorPrimary"
        android:outlineAmbientShadowColor="@color/colorPrimary"
        app:tint="@color/white"
        app:fabSize="mini"
        app:srcCompat="@drawable/ic_zoom15" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabZoom1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="4dp"
        app:backgroundTint="@color/colorPrimary"
        android:outlineAmbientShadowColor="@color/colorPrimary"
        app:tint="@color/white"
        app:srcCompat="@drawable/ic_zoom1"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>


